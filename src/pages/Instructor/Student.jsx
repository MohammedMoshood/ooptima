import React from "react";
import SingleStudent from "../../components/SingleStudent";

const Student = ({cs}) => {
  return (
    <main>
      <SingleStudent cs={cs} />
    </main>
  );
};

export default Student;
